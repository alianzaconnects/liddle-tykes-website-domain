# Render Blueprint — Static Site (CDN)
# Plain npm. Render auto-installs deps from package-lock.json,
# then runs the build command. dist/ is published to a global CDN.
services:
  - type: web
    name: liddle-tykes-website
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist

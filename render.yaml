# Render Blueprint — Static Site (CDN)
# https://render.com/docs/deploy-a-vite-application
services:
  - type: web
    name: liddle-tykes-website
    runtime: static
    # deps: Render runs pnpm install (frozen) when pnpm-lock.yaml is present
    buildCommand: pnpm run build
    staticPublishPath: dist
